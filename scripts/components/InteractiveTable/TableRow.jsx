define (['react-with-addons'], function(React) {
  var TableRow = React.createClass({
    render : funtion() {
      return (
        <table>
          <TableRow />
        </table>
      );
    }
  });

  return TableRow;
});
