define (['react-with-addons'], function(React) {
  var TableCell = React.createClass({
    render : funtion() {
      return (
        <table>
          <TableRow />
        </table>
      );
    }
  });

  return TableCell;
});
